# Minimal Mistakes 主题 - 分类和侧边栏说明

## 📂 文章分类机制

### 1. 如何设置文章分类

在每篇文章的 **Front Matter**（文件开头的 YAML 配置）中，使用 `categories` 字段来设置分类：

```yaml
---
layout: single
title: "文章标题"
date: 2025-01-16 10:00:00 +0800
categories: 技术 教程  # 空格分隔，可以有多个分类
tags: [Jekyll, 博客]
---
```

### 2. 分类的格式

- **多个分类**：使用空格分隔，例如 `categories: 技术 教程`
- **单个分类**：直接写，例如 `categories: 技术`
- **分类命名**：建议使用中文，简单明了

### 3. 分类在哪里体现？

#### ① 文章页面
- 文章顶部会显示分类标签（如：**技术**、**教程**）
- 点击分类标签可以查看该分类下的所有文章

#### ② 分类归档页面
- 访问 `/categories/` 可以看到所有分类
- 每个分类下会列出该分类的所有文章

#### ③ 文章底部
- 相关文章推荐可能会基于分类

#### ④ 首页
- 文章列表中会显示分类信息

### 4. 示例

```yaml
# 技术文章
categories: 技术 前端

# 教程文章
categories: 教程 JavaScript

# 生活随笔
categories: 随笔 生活

# 工作相关
categories: 工作 项目管理
```

### 5. 推荐的分类体系

参考 `_config.yml` 中的分类说明：

- **技术**：技术相关文章
- **教程**：教程类文章
- **笔记**：学习笔记
- **随笔**：日常随笔
- **项目**：项目相关
- **工具**：工具推荐和使用
- **前端**：前端技术
- **后端**：后端技术
- **算法**：算法相关
- **生活**：生活相关

---

## 👤 侧边栏（作者信息）配置

### 1. 侧边栏的控制方式

侧边栏通过以下方式控制：

#### ① 全局配置（_config.yml）

```yaml
author:
  name: "Tec-Wang"
  avatar: /assets/images/avatar.jpg  # 头像路径
  bio: "个人简介"
  location: "China"
  email: "your-email@example.com"
  links:
    - label: "GitHub"
      icon: "fab fa-github"
      url: "https://github.com/Tec-Wang"
```

#### ② 默认值配置

```yaml
defaults:
  - scope:
      path: ""
      type: posts
    values:
      author_profile: true  # 默认启用作者侧边栏
```

#### ③ 单篇文章控制

在每篇文章的 Front Matter 中：

```yaml
---
layout: single
title: "文章标题"
author_profile: true  # 显示侧边栏
# 或
author_profile: false  # 不显示侧边栏
---
```

### 2. 为什么侧边栏不显示？

#### ❌ 可能的原因：

1. **头像文件不存在**
   - 如果 `avatar` 路径的文件不存在，侧边栏可能不显示
   - 检查：`assets/images/avatar.jpg` 是否存在
   - 解决：添加头像文件或使用占位符

2. **`author_profile: false`**
   - 文章中明确设置为 `false`
   - 检查文章的 Front Matter

3. **作者信息配置不完整**
   - `_config.yml` 中的 `author` 配置缺失或不完整

4. **布局问题**
   - 使用了错误的布局
   - Minimal Mistakes 主题使用 `layout: single`，不是 `post`

### 3. 排查步骤

#### Step 1: 检查头像文件
```bash
# 确保头像文件存在
ls assets/images/avatar.jpg
```

如果不存在，创建头像或修改配置：
```yaml
# _config.yml
author:
  avatar: /assets/images/avatar.jpg  # 确保路径正确
```

#### Step 2: 检查文章配置
确保文章的 Front Matter 中有：
```yaml
---
layout: single
author_profile: true
---
```

#### Step 3: 检查全局配置
确保 `_config.yml` 中有：
```yaml
author:
  name: "Tec-Wang"
  avatar: /assets/images/avatar.jpg
  bio: "个人简介"
  # ... 其他配置
```

#### Step 4: 检查默认值
确保 `defaults` 配置正确：
```yaml
defaults:
  - scope:
      path: ""
      type: posts
    values:
      author_profile: true
```

### 4. 侧边栏显示的内容

当 `author_profile: true` 时，侧边栏会显示：

- ✅ **头像**：`author.avatar` 设置的图片
- ✅ **姓名**：`author.name`
- ✅ **简介**：`author.bio`
- ✅ **位置**：`author.location`（如果有）
- ✅ **邮箱**：`author.email`（如果有）
- ✅ **社交链接**：`author.links` 中配置的链接
- ✅ **文章分类/标签**：当前文章的分类和标签
- ✅ **相关文章**：同一分类或标签的其他文章

### 5. 完整示例

#### 正确的文章配置：

```yaml
---
layout: single
title: "Minimal Mistakes 主题完整使用指南"
date: 2025-01-16 10:00:00 +0800
categories: 技术 教程
tags: [Jekyll, Minimal Mistakes, 博客, 教程]
author: Tec-Wang
author_profile: true  # 启用侧边栏
---
```

#### 正确的全局配置：

```yaml
# _config.yml
author:
  name: "Tec-Wang"
  avatar: /assets/images/avatar.jpg
  bio: "一只热爱编程的🐟，喜欢研究各种技术，喜欢分享自己的学习心得，喜欢帮助别人，喜欢写作。"
  location: "China"
  email: "wangzheng4j@qq.com"
  links:
    - label: "GitHub"
      icon: "fab fa-github"
      url: "https://github.com/Tec-Wang"
```

---

## 🔧 快速修复

如果"Minimal Mistakes 主题完整使用指南"没有显示侧边栏：

### 方法 1: 确保头像文件存在

```bash
# 创建头像文件（如果没有）
# 或者修改配置使用在线头像
```

### 方法 2: 在文章中明确启用

检查 `_posts/2025-01-16-jekyll-minimal-mistakes-theme-guide.md`，确保有：

```yaml
---
layout: single
author_profile: true  # 确保这行存在
---
```

### 方法 3: 临时使用占位符头像

如果暂时没有头像，可以修改 `_config.yml`：

```yaml
author:
  avatar: https://via.placeholder.com/200x200  # 临时使用占位符
```

---

## 📝 总结

### 分类机制
- ✅ 在 Front Matter 中使用 `categories: 分类1 分类2`
- ✅ 空格分隔多个分类
- ✅ 分类会在文章页面、归档页面显示
- ✅ 访问 `/categories/` 查看所有分类

### 侧边栏机制
- ✅ 通过 `author_profile: true` 控制显示
- ✅ 需要 `author` 配置完整
- ✅ 需要头像文件存在（或使用占位符）
- ✅ 全局默认值 + 单篇文章配置都可以控制

---

如果还有问题，请检查：
1. 头像文件是否存在
2. `author_profile` 是否为 `true`
3. `_config.yml` 中的 `author` 配置是否完整
4. Jekyll 服务器是否重启（修改 `_config.yml` 后需重启）

